name: Cutey Honey FX KR ALPHA v0.15 Runner
permissions:
  contents: write
on:
  push:
    branches: [tmp-cutey-poc01]
    paths:
      - '.github/workflows/cutey_full_alpha_v015_runner.yml'
      - 'scripts/cutey_fx/build_alpha_v015.sh'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp-cutey-poc01
      - name: Build package
        run: bash scripts/cutey_fx/build_alpha_v015.sh
      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: CuteyHoneyFX_KR_ALPHA_v0.15
          path: /tmp/CuteyHoneyFX_KR_ALPHA_v0.15.zip
          compression-level: 0
      - name: Commit extraction and build summaries
        run: |
          mkdir -p analysis/cutey_full
          cp /tmp/pkg/EXTRACTION_SUMMARY.json analysis/cutey_full/EXTRACTION_CONTROL_v015.json
          cp /tmp/pkg/BUILD_INFO.json analysis/cutey_full/BUILD_INFO_v015.json
          cp /tmp/pkg/TRANSLATION_PROGRESS.csv analysis/cutey_full/TRANSLATION_PROGRESS_v015.csv
          cp /tmp/pkg/TEXT_CONTROL_UNIQUE.csv analysis/cutey_full/TEXT_CONTROL_UNIQUE_v015.csv
          cp /tmp/pkg/TEXT_CONTROL_INVENTORY.csv analysis/cutey_full/TEXT_CONTROL_INVENTORY_v015.csv
          git config user.name 'Cutey full builder'
          git config user.email 'actions@users.noreply.github.com'
          git add analysis/cutey_full
          git diff --cached --quiet || git commit -m 'analysis: Cutey KR alpha v0.15 inventory [skip ci]'
          git push origin HEAD:tmp-cutey-poc01
